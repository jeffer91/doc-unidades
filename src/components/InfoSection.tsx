import React,{useEffect,useState} from 'react';
export function InfoSection({ctx}:{ctx:any}){const [stats,setStats]=useState({rows:0});useEffect(()=>{if(ctx.period)window.docUnits.stats({periodId:ctx.period.id,unitId:ctx.unit,processId:ctx.process.id,documentId:ctx.document.id}).then(setStats)},[ctx.period?.id,ctx.unit,ctx.process.id,ctx.document.id]);return <div className="work-card"><div className="section-heading"><div><h2>Información</h2><p>Estado operativo del documento seleccionado.</p></div></div><div className="info-grid"><div className="info-tile"><span>Documento</span><strong>{ctx.document.label}</strong></div><div className="info-tile"><span>Proceso</span><strong>{ctx.process.label}</strong></div><div className="info-tile"><span>Registros en matrices</span><strong>{stats.rows}</strong></div><div className="info-tile"><span>Estado</span><strong className={stats.rows>0?'ok':'pending'}>{stats.rows>0?'En trabajo':'Pendiente'}</strong></div></div><div className="hint-box">Las matrices, textos y plantillas se guardan en SQLite local y permanecen aislados por período, unidad, proceso y documento.</div></div>}
